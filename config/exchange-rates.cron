# Install with: crontab config/exchange-rates.cron
# Adjust the project path and PHP binary for the deployment host.
5 0 * * * cd /path/to/project && /usr/bin/php bin/console app:exchange-rates:update --env=prod >> var/log/exchange-rates-cron.log 2>&1
